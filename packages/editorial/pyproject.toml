[project]
name = "stream-editor-editorial"
version = "0.1.0"
description = "Editorial planning for StreamEditor AI"
requires-python = ">=3.12"
dependencies = [
    "stream-editor-contracts",
    "stream-editor-vault",
]

[tool.uv.sources]
stream-editor-contracts = { workspace = true }
stream-editor-vault = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stream_editor"]
