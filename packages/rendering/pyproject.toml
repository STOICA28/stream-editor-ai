[project]
name = "stream-editor-rendering"
version = "0.1.0"
description = "EditPlan renderer for StreamEditor AI"
requires-python = ">=3.12"
dependencies = [
    "stream-editor-contracts",
    "stream-editor-storage",
]

[tool.uv.sources]
stream-editor-contracts = { workspace = true }
stream-editor-storage = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stream_editor"]
