[project]
name = "stream-editor-contracts"
version = "0.1.0"
description = "Shared Pydantic schemas for StreamEditor AI"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stream_editor"]